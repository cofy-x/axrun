Create a complete Python command-line program at `/workspace/greeter.py`.

The command `python3 greeter.py NAME` must print `Hello, NAME!` followed by one newline. The command `python3 greeter.py --shout NAME` must print the same greeting in uppercase. Invalid arguments and an empty name must write a diagnostic to stderr and exit with status 2. Do not use the network or external packages.
