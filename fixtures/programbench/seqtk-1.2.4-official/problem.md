Rebuild the seqtk command-line behavior from its bundled documentation and executable.
Provide source files and an offline compile.sh that creates /workspace/executable.
The evaluator runs in a fresh environment and consumes only the submitted archive.

Use only behavioral observations of the provided executable and its bundled documentation.
Do not consult source code, repositories, mirrors, package registries, or unit tests, and do
not use internet access. Do not copy or include a pre-existing implementation or executable;
the submitted source and compile.sh must be your reconstruction from those observations.
