# Rebuild tty-clock

Recreate the program available as `./executable` in this workspace.

You may execute the reference program through its normal user interface to learn its behavior, but
you must not read, copy, trace, disassemble, or otherwise inspect its implementation. Work without
network access. Produce original source code and a `compile.sh` that builds a fresh
`./executable` from that source in the cleanroom environment.

Your submission is the workspace tree after the seed-owned reference `executable` is removed. Do
not include prebuilt objects or binaries. The evaluator deletes any submitted `./executable`, runs
`compile.sh` offline, and evaluates the newly built executable with hidden behavioral tests.
